plugins {
    id 'java'
    id 'kr.entree.spigradle' version '2.2.4'
}

group 'org.sample'
version '1.0-SNAPSHOT'
apply plugin: 'groovy'

repositories {
    maven { url 'https://jitpack.io' }
    maven { url = uri("https://hub.spigotmc.org/nexus/content/repositories/snapshots/") }
    maven { url = uri("https://maven.enginehub.org/repo/") }
    maven { url = uri("https://repo.dmulloy2.net/repository/public/") }
}
dependencies {
    compileOnly 'org.spigotmc:spigot-api:1.19.2-R0.1-SNAPSHOT' // Bukkit
    compileOnly 'org.spigotmc:spigot:1.19.2-R0.1-SNAPSHOT' // Bukkit

    compileOnly fileTree(dir: 'api', include: ['*.jar'])
}

jar {
    destinationDirectory.set(file('D:\\Project\\minecraft\\Commission\\플러그인테스트\\1.19.2\\plugins'))
}
